import { VctStatusDirective } from './vct-status.directive';

describe('VctStatusDirective', () => {
  it('should create an instance', () => {
    const directive = new VctStatusDirective(null,null);
    expect(directive).toBeTruthy();
  });
});
