import { VctStatusPipe } from './vct-status.pipe';

describe('VctStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new VctStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
